"""AgentOS Security Intelligence Foundation.

Architecture:

    Action → Context Builder → Intelligence Assessment → Deterministic Policy Engine → Decision

The intelligence layer ANALYZES, CLASSIFIES, EXPLAINS, and RECOMMENDS.
It NEVER authorizes. The deterministic policy engine remains the final authority
(absolute security rule: AI may recommend; policy decides).

Every intelligence result is explainable: source, evidence, confidence, reasoning,
timestamp, engine version, and provenance are always present.
"""
