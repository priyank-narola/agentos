"use client";

import { useEffect, useState } from "react";
import { Tool, api } from "@/lib/api";
import { RegistryShell, StateMessage, StatusPill } from "@/components/registry-shell";

export default function ToolsPage() { const [tools, setTools] = useState<Tool[]>([]); const [error, setError] = useState<string | null>(null); const [loading, setLoading] = useState(true); useEffect(() => { api.tools().then(setTools).catch((reason: Error) => setError(reason.message)).finally(() => setLoading(false)); }, []); return <RegistryShell title="Tool registry" eyebrow="Capability inventory"><p className="mb-6 max-w-xl text-sm leading-6 text-slate-500">Tools group protected capabilities. Each action has its own risk classification and must not inherit authorization from the tool.</p>{loading && <StateMessage>Loading registered tools…</StateMessage>}{error && <StateMessage tone="error">Unable to load tools. Confirm the FastAPI service is running. {error}</StateMessage>}{!loading && !error && tools.length === 0 && <StateMessage>No tools registered yet.</StateMessage>}{tools.length > 0 && <div className="grid gap-4 md:grid-cols-2">{tools.map((tool) => <div key={tool.id} className="border border-slate-200 bg-white p-6"><div className="flex items-start justify-between gap-4"><div><h2 className="font-semibold text-ink">{tool.name}</h2><p className="mt-2 text-sm leading-6 text-slate-500">{tool.description}</p></div><StatusPill value={tool.status} /></div><p className="mt-6 border-t border-slate-100 pt-4 text-xs text-slate-400">Each action under this tool carries its own risk classification.</p></div>)}</div>}</RegistryShell>; }
