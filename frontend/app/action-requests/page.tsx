"use client";

import Link from "next/link";
import { useEffect, useState } from "react";
import { ActionRequest, api } from "@/lib/api";
import { RegistryShell, StateMessage, StatusPill } from "@/components/registry-shell";

export default function ActionRequestsPage() { const [requests, setRequests] = useState<ActionRequest[]>([]); const [error, setError] = useState<string | null>(null); useEffect(() => { api.actionRequests().then(setRequests).catch((reason: Error) => setError(reason.message)); }, []); return <RegistryShell title="Action requests" eyebrow="Gateway evidence"><p className="mb-6 text-sm leading-6 text-slate-500">Persisted gateway submissions and their separate server-generated decisions.</p>{error && <StateMessage tone="error">Unable to load action requests. {error}</StateMessage>}{!error && requests.length === 0 && <StateMessage>No gateway requests recorded yet.</StateMessage>}{requests.length > 0 && <div className="overflow-hidden border border-slate-200 bg-white"><div className="grid grid-cols-[1.3fr_1fr_0.9fr_0.9fr] gap-4 border-b border-slate-200 bg-slate-50 px-5 py-3 text-xs font-semibold uppercase tracking-wide text-slate-500"><span>Agent / action</span><span>Resource</span><span>Decision</span><span>Requested</span></div>{requests.map((request) => <Link key={request.id} href={`/action-requests/${request.id}`} className="grid grid-cols-[1.3fr_1fr_0.9fr_0.9fr] gap-4 border-b border-slate-100 px-5 py-4 text-sm last:border-0 hover:bg-slate-50"><span><strong className="block font-medium text-ink">{request.agent_name}</strong><small className="text-xs text-slate-400">{request.action_name}</small></span><span className="text-slate-500">{request.resource_key}</span><span><StatusPill value={request.decision ?? request.status} /></span><span className="text-xs text-slate-400">{new Date(request.requested_at).toLocaleString()}</span></Link>)}</div>}</RegistryShell>; }
