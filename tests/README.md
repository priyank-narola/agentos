# Test Workspace

Cross-service and end-to-end tests will live here when the runtime action gateway is introduced. Phase 1 keeps its executable API tests next to the backend application in `backend/tests/`.
